apiVersion: batch/v1
kind: Job
metadata:
  name: mongodb-init-reports
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-delete-policy: before-hook-creation
    helm.sh/hook-weight: "10"
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: mongo-client
          image: mongo:7.0
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          command:
            - sh
            - -c
            - |
              echo "Waiting for MongoDB..."
              until mongosh --host mongodb \
                -u "$MONGO_INITDB_ROOT_USERNAME" \
                -p "$MONGO_INITDB_ROOT_PASSWORD" \
                --authenticationDatabase admin \
                --eval "db.adminCommand('ping')" \
                --quiet; do
                echo "still waiting..."
                sleep 5
              done
              echo "MongoDB is ready, creating user..."
              mongosh --host mongodb \
                -u "$MONGO_INITDB_ROOT_USERNAME" \
                -p "$MONGO_INITDB_ROOT_PASSWORD" \
                --authenticationDatabase admin <<EOF
              db = db.getSiblingDB("$REPORTS_DB");
              db.createUser({
                user: "$REPORTS_USER",
                pwd: "$REPORTS_PASSWORD",
                roles: [{ role: "readWrite", db: "$REPORTS_DB" }]
              });
              EOF
              echo "User created successfully."
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: {{ .Values.infra.mongodb.env.MONGO_INITDB_ROOT_USERNAME | quote }}
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: {{ .Values.infra.mongodb.env.MONGO_INITDB_ROOT_PASSWORD | quote }}
            - name: REPORTS_DB
              value: {{ .Values.global.mongodb.reports.database | quote }}
            - name: REPORTS_USER
              value: {{ .Values.global.mongodb.reports.user | quote }}
            - name: REPORTS_PASSWORD
              value: {{ .Values.global.mongodb.reports.password | quote }}